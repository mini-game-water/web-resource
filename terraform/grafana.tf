# Grafana OSS is self-hosted on EC2 (installed via user_data.sh).
# See user_data.sh for Grafana installation, datasource provisioning, and dashboard setup.
# ALB routes /grafana/* to port 3000 on EC2.
